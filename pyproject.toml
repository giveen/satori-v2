[tool.poetry]
name = "satori-offline"
version = "0.1.0"
description = "Passive OS fingerprinting toolkit (offline + live)"
authors = ["Satori Team <devnull@example.com>"]

[tool.poetry.dependencies]
python = "^3.11"
dpkt = "^1.9.0"
lmdb = "^1.4"
scapy = {version = ">=2.5", optional = true}

[tool.poetry.extras]
live = ["scapy"]

[tool.poetry.dev-dependencies]
pytest = "^7.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
